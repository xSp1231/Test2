for(i=1; i<=n*m; i++) {
		for(j=1; j<=n*m; j++) {
			for(k=1; k<=n*m; k++) {
				if(a[j][i]==1&&a[i][k]==1) {//循环里面三个变量的顺序不能够交换  因为 最先进行jk的更新，所以要尽可能的保证jk的准确 所以要先更新jk 顺序很重要
 
					a[j][k]=1;
				}
			}
		}
	}